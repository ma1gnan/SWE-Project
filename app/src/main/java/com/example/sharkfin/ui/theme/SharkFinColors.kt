package com.example.sharkfin.ui.theme

import androidx.compose.ui.graphics.Color

// ── BACKGROUNDS ──────────────────────────────────────────
val SharkBase        = Color(0xFF0E1117)  // App background
val SharkSurface     = Color(0xFF161D27)  // Cards, sheets
val SharkSurfaceHigh = Color(0xFF1C2535)  // Elevated cards
val SharkOverlay     = Color(0xFF243044)  // Modals, overlays

// ── BORDERS ───────────────────────────────────────────────
val SharkBorderSubtle  = Color(0xFF1F2D3D)  // Subtle dividers
val SharkBorderMedium  = Color(0xFF2A3A50)  // Card borders
val SharkBorderStrong  = Color(0xFF3D5068)  // Active borders

// ── GOLD ACCENT — THE SOUL OF THE APP ────────────────────
val SharkGold        = Color(0xFFE8B84B)  // Primary accent
val SharkGoldSoft    = Color(0xFFD4A43C)  // Pressed state
val SharkGoldDim     = Color(0xFF8A6E2D)  // Disabled/muted
val SharkGoldGlow    = Color(0x26E8B84B)  // 15% alpha glow

// ── TEXT HIERARCHY ────────────────────────────────────────
val SharkTextPrimary   = Color(0xFFFFFFFF)   // Main content
val SharkTextSecondary = Color(0xFFB0BEC5)   // Supporting info
val SharkTextMuted     = Color(0xFF546E7A)   // Labels, hints
val SharkTextDisabled  = Color(0xFF2E3E4E)   // Inactive

// ── SEMANTIC COLORS ───────────────────────────────────────
val SharkPositive      = Color(0xFF4CAF82)   // Income, gains
val SharkNegative      = Color(0xFFE05C5C)   // Expenses, losses
val SharkWarning       = Color(0xFFE8944B)   // Bills, alerts
val SharkInfo          = Color(0xFF4BA3E8)   // Informational
